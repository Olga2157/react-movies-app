export type FilterButtonInfo = {
  buttonId: string;
  buttonText: string;
  };

export type MovieInfo = {
    id: number;
    title: string;
    posterPath: string;
    year: number;
    genre: string;
  }

  export type PaginationItemInfo = {
    pageKey: number;
    active: boolean;
  } 
