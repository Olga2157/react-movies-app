export type FilterButtonInfo = {
  buttonId: string;
  buttonText: string;
  };

  export type MovieInfo = {
    // movieId: number;
    title: string;
    posterPath: string;
    year: number;
    genre: string;
  }
